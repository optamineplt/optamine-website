{
  "builds": [
    {
      "src": "mkdocs.yml",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/site/$1"
    }
  ],
  "buildCommand": "python -m pip install mkdocs mkdocs-material && python -m mkdocs build",
  "outputDirectory": "site"
}